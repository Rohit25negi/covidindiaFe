import { TabledirectiveDirective } from './tabledirective.directive';

describe('TabledirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TabledirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
